package main

func main() {
	// str := "Hello"

	//length of String
	// fmt.Println("len is", len(str))

	//Reversed String
	// for i := len(str) - 1; i >= 0; i-- {
	// 	fmt.Print(string(str[i]))
	// }

	// str := "asdsa"

	// newStr := ""

	// for i := len(str) - 1; i >= 0; i-- {
	// 	newStr += string(str[i])
	// }
	// fmt.Println("str is", str)
	// fmt.Println("new Str is", newStr)
	// fmt.Println("is Palindrome", str == newStr)

	// s := "Siddhu Surwade"
	// fmt.Println(strings.Trim(s, " "))
	// fmt.Println(strings.ToLower(strings.ReplaceAll(s, " ", "")))

}
